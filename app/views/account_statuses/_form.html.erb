<%= form_for(account_status) do |f| %>
  <% if account_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account_status.errors.count, "error") %> prohibited this account_status from being saved:</h2>

      <ul>
      <% account_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :note %>
    <%= f.text_area :note %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
