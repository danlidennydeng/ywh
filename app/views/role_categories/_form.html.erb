<%= form_for(role_category) do |f| %>
  <% if role_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role_category.errors.count, "error") %> prohibited this role_category from being saved:</h2>

      <ul>
      <% role_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :note %>
    <%= f.text_area :note %>
  </div>

  <div class="field">
    <%= f.label :edited_by %>
    <%= f.number_field :edited_by %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
